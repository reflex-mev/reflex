# Environment variables for ReflexRouter deployment

# Optional: ReflexQuoter address to set after deployment
# Leave empty to skip setting quoter during deployment
REFLEX_QUOTER_ADDRESS=

# Optional: Verify contract on Etherscan (true/false)
VERIFY_CONTRACT=false

# Required if VERIFY_CONTRACT is true
ETHERSCAN_API_KEY=

# Optional: Gas price in gwei (leave empty for automatic)
GAS_PRICE=

# Optional: Gas limit (leave empty for automatic estimation)
GAS_LIMIT=

# Network configuration
# RPC_URL will be provided via command line --rpc-url
# PRIVATE_KEY will be provided via command line --private-key

# Example usage:
# forge script script/deploy-reflex-router/DeployReflexRouter.s.sol \
#   --rpc-url $RPC_URL \
#   --private-key $PRIVATE_KEY \
#   --broadcast \
#   --verify
